[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ipymini"
dynamic = ["version"]
description = "A small, readable, Python-only Jupyter kernel with strong IPython parity."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "comm>=0.2.3",
  "ipython>=8.18",
  "kernmini>=0.1.0",
]

[project.optional-dependencies]
dev = [
  "jupyter_client>=8.6",
  "pytest>=7.4",
  "pytest-asyncio>=1.0",
  "pytest-xdist",
  "pytest-timeout",
  "conkernelclient>=0.0.17",
  "matplotlib>=3.8",
  "ipykernel>=6.29",
  "debugpy>=1.8",
  "murmurhash2",
]
dev = [
  "jupyter_client>=8.6",
  "pytest>=7.4",
  "pytest-asyncio>=1.0",
  "pytest-xdist",
  "pytest-timeout",
  "conkernelclient>=0.0.17",
  "matplotlib>=3.8",
  "ipykernel>=6.29",
  "debugpy>=1.8",
  "murmurhash2",
  "fastship",
  "build",
  "twine",
]

[project.scripts]
ipymini = "ipymini.__main__:main"

[tool.setuptools.dynamic]
version = { attr = "ipymini.__version__" }

[tool.setuptools.packages.find]
include = ["ipymini", "ipymini.*"]

[tool.setuptools.data-files]
"share/jupyter/kernels/ipymini" = ["share/jupyter/kernels/ipymini/kernel.json"]

[tool.chkstyle]
skip-folder-re = "meta"
